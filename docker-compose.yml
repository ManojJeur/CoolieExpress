version: '3.8'

services:
  mysql-db:
    image: mysql:8.0
    container_name: coolie_express_db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: coolie_express
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data:
